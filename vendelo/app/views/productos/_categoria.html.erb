<%# está generando un enlace que envía el categoria_id como parámetro en la URL %>
<%= link_to productos_path(categoria_id: categoria.id), class:'categoria' do %> 

<%= categoria.nombre  %> |

<% end %>